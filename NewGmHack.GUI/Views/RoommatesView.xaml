<UserControl x:Class="NewGmHack.GUI.Views.RoommatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewGmHack.GUI.Views"
             xmlns:viewModels="clr-namespace:NewGmHack.GUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <viewModels:RoommatesViewModel />
    </UserControl.DataContext>
    <Grid>
             <ListView ItemsSource="{Binding Roommates}" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8" BorderBrush="#DDD" BorderThickness="1" Padding="10" Margin="5" Background="White">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="#333"/>
                            <TextBlock Text="{Binding ItemName}" FontSize="14" Foreground="#666" Margin="0,2,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="Player ID: " FontWeight="SemiBold" Foreground="#888"/>
                                <TextBlock Text="{Binding PlayerId}" Foreground="#444"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Item ID: " FontWeight="SemiBold" Foreground="#888"/>
                                <TextBlock Text="{Binding ItemId}" Foreground="#444"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
