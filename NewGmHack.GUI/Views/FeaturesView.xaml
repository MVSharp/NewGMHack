<UserControl x:Class="NewGmHack.GUI.Views.FeaturesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewGmHack.GUI.Views"
             xmlns:vm="clr-namespace:NewGmHack.GUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             VerticalAlignment="Stretch"
             HorizontalAlignment="Stretch">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 🔄 Refresh Button -->
        <Button Content="🔄 Refresh"
                Command="{Binding RefreshCommand}"
                HorizontalAlignment="Right"
                Margin="0,0,10,10"
                Padding="8,4"
                FontSize="14"
                Background="#E0E0E0"
                BorderBrush="#CCC"
                BorderThickness="1"
                Cursor="Hand"
                Grid.Row="0"/>

        <!-- Features List -->
        <Border Grid.Row="1" CornerRadius="8" Padding="15" BorderBrush="#DDD" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Features}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" 
                                      IsChecked="{Binding IsEnabled}" 
                                      Margin="10"
                                      Padding="5"
                                      FontSize="14"
                                      Foreground="#333"
                                      Background="#FAFAFA"
                                      BorderBrush="#CCC"
                                      BorderThickness="1"
                                      Cursor="Hand"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
