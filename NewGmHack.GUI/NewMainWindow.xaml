<mah:MetroWindow x:Class="NewGmHack.GUI.NewMainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                 mc:Ignorable="d"
                 Title="SD Hack By Michael Van" Height="800" Width="1200"
                 GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                 ResizeMode="CanResizeWithGrip"
                 WindowStartupLocation="CenterScreen"
                 Background="#0b0c10">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- WebView2 Control -->
        <wv2:WebView2 x:Name="DashboardWebView" 
                      DefaultBackgroundColor="#0b0c10"/>
        
        <!-- Loading Overlay (Visible until navigation completes if needed, or handled by web content) -->
        
    </Grid>
</mah:MetroWindow>
